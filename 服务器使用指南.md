# 服务器使用指南

本文中提到的部分路径是基于📁`/mnt/HDD1/archives`的相对路径，如📁`files/example`或📄`models/example.stl`

---

## 服务器地址

- **内网 IP**：`192.168.5.5`
- **校园网 IP**：`10.11.18.2`（经过路由器端口转发）
- 校外网打开 atrust 即可按校园网方式使用

---

## 公共路径

| 路径                     | 说明                                                       |
| ---------------------- | -------------------------------------------------------- |
| 📁`/opt`               | 主要存放各类软件包，包括 Anaconda 等，所有用户都有读写权限                       |
| 📁`/mnt/HDD1/你的用户名`    | 存放实验数据，除非授权（见 *UAC 用户访问控制* 章节）否则别人无法访问，也是用户个人 FTP 服务的根目录 |
| 📁`/mnt/HDD1/archives` | 存放实验室公共资料，也是 Samba 服务的根目录                                |
| 📁`/mnt/SSD_tmp`       | 存放大型计算程序的临时文件，以防止占满其他目录                                  |

---

## 新用户注意事项

- 请先了解 Linux 的基础知识，主要包括大致逻辑、文件结构、bash 基础用法
  
  🔗 [从零开始学习 Linux Ubuntu](https://geek-blogs.com/blog/learn-linux-ubuntu/)

- 除非有需要，否则尽量**不要**注册管理员（sudo）账户

- 账户名一般默认是**全小写**，即账户名显示的是 ABC，但登陆的时候要输入 `abc`

- ⚠️ Linux 终端的复制粘贴键和其他系统**不同**！

- Linux 可定制性非常高。如果有想要的功能或者不顺手/不顺眼的地方，可以搜索并了解改进/美化方案

- ⚠️ **不要随便删你不知道是什么的东西！**

---

## 程序和服务

这些都是服务器环境中已有的，不必再次安装

### 可用程序

- **MATLAB**
  
  命令行输入 `matlab` 或将 📄`matlab.desktop` 复制到桌面使用
  
  📁`/mnt/HDD1/archives/files/matlab.desktop`

- **Python 及 Conda 环境**
  
  公共环境目录位于 📁`/opt/anaconda3`，可使用 `conda env config vars list` 查看环境描述
  
  构建私人环境可以使用 `prefix=` 参数指定路径。新建公共环境后可以使用如下命令创建描述：
  
  ```bash
  conda activate 你的环境
  conda env config vars set DESCRIPTION="这是用于VNC的Python环境"
  ```
  
  🔗 [conda 入门 — conda 25.1.1 文档](https://docs.conda.org.cn/projects/conda/en/stable/user-guide/getting-started.html)

- **FIJI (ImageJ)**
  
  可从程序菜单找到图标，安装了 FFMPEG 和 FFmpegVideoImportExport 插件用于支持带压缩的视频读写
  
  特调了 📄`ImageJ.cfg` 以修复显示 bug，见*疑难杂症*章节
  
  🔗 [Treasure 琛 - 知乎](https://www.zhihu.com/people/zhao-yu-chen-40-11)

- **VS Code、CMake、R Studio 等**
  
  可从程序菜单找到图标

- **FFMPEG（命令行）**
  
  一个极其强大的**视音频及图片编辑工具**命令行工具，可以直接命令行调用，也可通过其他编程语言的接口调用，或者让 AI 生成一条符合你需求的指令
  
  🔗 [FFmpeg 教程 — FFmpeg 教程文档](https://wklchris.github.io/blog/FFmpeg/index.html)

- **杂项**
  
  有 WPS、LibreOffice、QQ、Chrome、Edge、Blender、Fsearch、FreeFileSync、向日葵、Todest、Clash、V2Ray 等软件，安装或更新软件可能需要管理员权限

### 可用服务

- **输入法**
  
  中文输入法基于 **fcitx5**，按 `Shift + Ctrl` 激活/切换，也可以自己安装输入法或皮肤

- **备份系统**
  
  `deja-dup`：每周五备份服务器的程序环境，**不备份实验数据**！
  
  命令行输入 `deja-dup` 查看或恢复备份（用于抢救环境崩溃或系统环境恢复）

- **杀毒软件**
  
  CalmAV，有实时拦截和手动扫描功能
  
  > 有待进一步测试

- **计算排队**
  
  由于服务器性能有限，运行高占用程序时请排队
  
  **排队方法**：主要使用 `fcntl`，不同语言用法不同，请将锁文件的 umask 设置为 `000`
  
  可参考 📁`git/caiman_scripts.git` 中的用法
  
  锁文件位置：`LOCK_FILE = "/var/lock/large_file_runner.lock"`（模式为阻塞 Block）

- **定时任务和常驻服务**
  
  - Cron：🔗 [Linux Cron 定时任务 | 菜鸟教程](https://www.runoob.com/w3cnote/linux-cron-task.html) 
  - Systemd：有系统级的和个人级的，可以在系统启动/你个人账户登录的时候就启动，用于作为某些服务端 🔗 [Linux Systemd 基础教程](https://www.cnblogs.com/TangQF/articles/18172031)
  - Nohup：挂起的后台进程，不太建议常用，不好管理

- **Git**
  
  克隆仓库请使用 `git clone 地址`，本地代码仓库在 📁`/mnt/HDD1/archives/git`
  
  🔗 [Git 教程 | 菜鸟教程](https://www.runoob.com/git/git-tutorial.html)

- **Docker**
  
  ⚠️ Docker 目前官方网站和镜像站几乎全部都无法使用，请自备 VPN
  
  🔗 [Docker 教程 | 菜鸟教程](https://www.runoob.com/docker/docker-tutorial.html)

- **VirtualGL**
  
  用来运行某些需要 OpenGL 的图形化应用程序，特别是与 3D 渲染有关的
  
  ⚠️ 后端需要选择 `egl`，否则会出现帧率只有 1 的 bug

- **GPaste**
  
  剪贴板历史记录，按 `Ctrl + Alt + G` 可打开
  
  ⚠️ 新版的 `vncviewer` 会截获此快捷键，可以从其设置界面关掉它，四个全点灭即可
  
  ![vnc.png](pictures/server-guide/vnc.png)
  
  - **AI & Agent**：Claude Code 和自己编写的文档查询助手，还有一个本地部署的阿里 Qwen3-VL 4B，权重参数位于📂`/mnt/HDD1/llm/models--Qwen--Qwen3-VL-4B-Instruct`

---

## 使用指南

### SSH 使用

```bash
ssh 账户名@IP  # 例如: ssh zyyk78@192.168.5.5
```

输入密码，此过程密码**不显示，需要盲打**，随后即可登陆远程 shell

**VSCode** 支持 SSH 远程连接，可以在远程端直接连接，不需要额外准备工作

如果**需要让其他人连接你的账户，但是不希望告诉 TA 你的密码**，可以使用*密钥连接*：

1. 使用 `ssh-keygen` 命令生成一对公钥和私钥，然后一路回车
2. 从 📁`~/.ssh` 下面（或者你自定义找到的位置）找到生成的那一对密钥，把私钥文件发给登陆者，公钥文件上传服务器：`ssh-copy-id -i 公钥文件 账户名`
3. 登陆者使用密钥登陆，登陆方法因软件而异，需要自行查询

### VNC 使用

1. SSH 登陆后输入 `vncpasswd`，设置 8 位密码

2. SSH 登陆后编辑 `sudo nano /etc/tigervnc/vncserver.users`，添加用户名和编号
   
   ⚠️ 如果没有管理员权限请去找有权限的人

3. 启动服务：`systemctl start vncserver@:编号`

4. 停止服务：`systemctl stop vncserver@:编号` 或从 VNC 界面的菜单栏点击`注销`

5. 本机 VNC 客户端连接：`IP:端口号`（如 `192.168.5.5:5901`）
   
   可能随后会有某些提示，一路 OK 即可，输入你的密码即可连接

6. 桌面环境可以通过以下指令自定义修改，注销重新登陆后即可生效：
   
   ```bash
   echo "session=桌面环境" > ~/.config/tigervnc/config
   ```
   
   可用桌面环境有：
   
   - `gnome-flashback-metacity`（默认）
   - `ubuntu-xorg`（同物理机）
   - `xfce`（轻量化）

**图形界面注意事项**：

- 右键菜单可以从不同位置打开终端
- 以 `.` 开头的文件和文件夹是隐藏的，不会显示
- 桌面和菜单栏图标是一系列 📄`.desktop` 文件，可自行拖动、编辑或复制
- 此外，还有一个特殊的 `x0vncserver`，可以让接到物理显示器的界面，但是同一时刻只能有一个用户使用，具体使用方法请参考程序的说明

### FTP 使用

- **普通 FTP**：端口 `11451`，在*我的电脑*右键添加网络位置
- **SFTP**：需要用专用工具登录
- ⚠️ Windows 直接访问 FTP 可能出现文件名乱码和各种报错，因为其支持太差，可以使用其他工具

### SMB 使用

- **Windows 用户**：*我的电脑*页面中右键选择*添加网络位置*，地址为 `\\10.11.18.2\public`，选择以 `samba` 的身份登陆，密码是 8 位，需要用户自己猜
- **MacOS 用户**：点击*访达*顶部的*前往* → *连接服务器*，地址为 `smb://10.11.18.2/public`，其他同 Windows 用户

### 内部邮件使用

- `sendmail` 或 `mail` 指令可以收发内部邮件
- 收到邮件后会有桌面（或 VNC 界面）弹窗提示，可用于自动化提醒
- `mail` 指令可以查看自己的收件箱

### Agent 使用

- Claude Code：可以直接在命令行输入`claude` 启动，也可以用 VSCode 中的插件，其中需要将📄`files/cc_settings.json`中的内容制到📄`~/.claude/settings.json`中覆盖对应字段才能使用学校提供的 AI；CC 的其他设置请自行研究学习；
  
  推荐安装的 MCP 和插件：context7；deepwiki；filesystem；github；minimax（负责查询和图片识别，建议禁用 websearch）；remember 等
  
  其还支持Claude Code for VS Code，可以自行安装；安装好后记得设置其**Claude Process Wrapper** 为`/usr/bin/claude`；插件建议自定义安装与claude code cli相同的版本

- 文档查询 Agent：公众号`SHTU-JLAB`提问即可查询此 wiki 的内容，或者从网址🔗`www.t80bvm.cc.cd`查询（该网址可以从校园网内稳定访问，从其他环境访问需要挂梯子）

---

## 数据共享：UAC 用户访问控制

如果你有和其他人**共享数据**的需求，请前往学习 Ubuntu 的用户权限管理（见上文）和 ACL

🔗 [Linux ACL（访问控制列表）详解](https://geek-blogs.com/blog/linux-acl/)

💡如果只是你的部分数据要共享给某**一个**人，这里推荐一个不太会出问题的流程：

依次执行以下命令：

```bash
chgrp 对方账户名 -R /path/to/folder
chmod g+s -R /path/to/folder
setfacl -R -m u:你的用户名:rwx,g::rwx,d:o:rx \
  -m d:u:你的用户名:rwx,d:g::rwx,d:o:rx /path/to/folder
```

---

## 疑难杂症

### VNC 下某些程序显示白屏或卡住的问题

- Java 程序加参数：`-Dsun.java2d.xrender=false`
- 使用 VirtualGL：`vglrun -d egl 你需要用的程序`
- 其他碰到再说

### 运行大型计算程序时 VNC 界面闪退/重启

⚠️ 一般是因为爆内存导致 Killer 启动了。请想办法控制内存占用而非关闭 killer，否则可能会导致卡死及更严重的事故

### 计算排队报错

⚠️ 请确认锁文件的权限！

### C++ 版本不匹配等疑难杂症

请善用 `update-alternatives`：🔗 [update-alternatives 命令详解](https://pengtech.net/linux/linux_update_alternatives.html)

### 更新系统或程序后 Matlab 出问题

如果其他人能运行，尝试：

```bash
rm -rf ~/.matlab
rm -rf ~/.MathWorks
```

⚠️ 这样会使你的账号和配置信息丢失，不过那些还可以再设置

---

## 自动测试

测试 build_index 自动 commit。
