# 服务器维护指南

## 基本信息

- 系统版本：Ubuntu 24.04 LTS

- 硬件配置：
  
  2 * Intel(R) Xeon(R) Gold 6230R 共 26x2x2=104 threads
  
  2 * NVIDIA RTX 4090D
  
  512G RAM
  
  1 TB SSD，分为四个区，分别挂载于`/boot/efi--105MB`  `/boot--524MB` `/--374GB` `/home--650GB`
  
  256 GB SSD，挂在于`/mnt/SSD_tmp`，用于存放CNMF算法生成的临时文件
  
  8 * 20 TB HDD，组建为RAID6 Array，挂载于`/mnt/HDD1`

- OOMD配置在 `/etc/systemd/oomd.conf.d/30-workstation.conf`

---

## UAC 用户访问控制

- `/opt` `/mnt/HDD1` `/mnt/HDD1/archives` `/mnt/SSD_tmp/caiman_data` 等文件夹设置了继承的ACL权限，以便公用

---

## 系统配置

- 内存压力限制: Systemd-oomd配置`/etc/systemd/oomd.conf.d/30-workstation.conf`
  
  对应cgroup设置在 `/etc/systemd/system/user@.service.d` 

- 内存占满限制：Kernel OOM-KILLER 配置在`/etc/sysctl.conf`

- 编辑了`/etc/pam.d/common-session`以便新用户自动初始化

- Systemd服务：`chatgpt-on-wechat.service`   

- Root Cron服务：`check-raid`

- 其他服务请自行查询或者使用`man 目标程序`查看使用手册和配置方法

### RAID阵列

- 使用 `mdadm` 组建的RAID6阵列，`metadata=1.2`，未使用 Intel VROC
- 查看状态: `cat /proc/mdstat`及各种mdadm指令
- 自动报警: 使用Root账户的Cron每日早9:00 自动检测，如果阵列报错则会给指定PushPlus推送警告，**一定要有人承接这个功能**

### 显卡驱动

现在可以正常工作，据说不要乱动就能用一辈子 😄

在重装VirtualGL的时候不要选择Restrict xxxxx,否则会导致某些程序调用显卡异常！如果搞错了可以`sudo /opt/VirtualGL/bin/vglserver_config`重新配置

---

> **特殊事项**: 要信仰全能的万机之神，全能的欧姆尼赛亚，机魂不悦则系统可能会出故障 🙏
