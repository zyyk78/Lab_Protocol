# 服务器维护指南

本文中提到的部分路径是基于📁`/mnt/HDD1/archives`的相对路径，如📁`files/example`或📄`models/example.stl`

---

## 基本信息

- **系统版本**：Ubuntu 24.04 LTS

- **硬件配置**：
  
  - 2 × `Intel(R) Xeon(R) Gold 6230R`（共 26×2×2=104 threads）
  - 2 × NVIDIA RTX 4090D
  - 512G RAM
  - 1 TB SSD，分为四个区，分别挂载于：
    - 📁`/boot/efi`（105MB）
    - 📁`/boot`（524MB）
    - 📁`/`（374GB）
    - 📁`/home`（650GB）
  - 256 GB SSD，挂载于 📁`/mnt/SSD_tmp`，用于存放 CNMF 算法生成的临时文件
  - 8 × 20 TB HDD，组建为 RAID6 Array，挂载于 📁`/mnt/HDD1`

- **OOMD 配置**在 📄`/etc/systemd/oomd.conf.d/30-workstation.conf`

---

## UAC 用户访问控制

- 📁`/opt`、📁`/mnt/HDD1`、📁`/mnt/HDD1/archives`、📁`/mnt/SSD_tmp/caiman_data` 等文件夹设置了继承的 ACL 权限，以便公用

---

## 系统配置

- **大部分 systemctl-daemon（systemd）**：配置脚本一般都在📄`/etc/对应名字.conf`或📁`/etc/对应名字.d`中

- **内存压力限制**：Systemd-oomd 配置在 📄`/etc/systemd/oomd.conf.d/30-workstation.conf`，对应 cgroup 设置在 📁`/etc/systemd/system/user@.service.d`

- **内存占满限制**：Kernel OOM-KILLER 配置在 📄`/etc/sysctl.conf`

- **Systemd 服务**：`shtu-proxy`，`wechat-bridge`，，`cloudflared`，`smartmontools` （alias `smartd`）

- **Root Cron 服务**：`check-raid`，`doc-update`

- **visudo**：额外设置Defaults 继承各种代理、额外设置所有用户免密码 `/usr/bin/systemctl start vncserver@??, /usr/bin/systemctl start vncserver@???`

- 其他服务请自行查询或者使用 `man 目标程序` 查看使用手册和配置方法

### 硬盘和 RAID 阵列

- 使用 `mdadm` 组建的 RAID6 阵列，`metadata=1.2`，未使用 Intel VROC

- 查看阵列状态：`cat /proc/mdstat` 及各种 mdadm 指令

- 查看硬盘状态：`sudo smartctl -H /dev/sdX` 及其他各种 S.M.A.R.T.指令

- ⚠️ **自动报警**：Root 账户的 Cron 每日早 9:00 自动检测阵列；smartd 每天凌晨 2-3 点会自动检查每块硬盘的健康状态；如果阵列或者硬盘警报则会向指定 PushPlus 账户推送警报信息，**一定要有人承接这个功能！**
  
  警报脚本放在📄`/usr/local/bin/check-raid.sh`和📄`/usr/local/bin/smartd_alert.sh`里面

### 显卡驱动

- 现在可以正常工作，据说不要乱动就能用一辈子 😄

- ⚠️ 在重装 VirtualGL 的时候**不要选择 Restrict xxxxx**，否则会导致某些程序调用显卡异常！如果搞错了可以 `sudo /opt/VirtualGL/bin/vglserver_config` 重新配置

### 服务配置

- **shtu-proxy**：systemd服务，一个将学校提供的 AI 接口转换为 CC 的格式，目前该接口有效期应该是到 2028年；学校提供的 API 类似 OpenAI 格式接口，但是缺少工具调用的逻辑，其他似乎同正常 OpenAI 兼容的工具格式；该项目目前来自于🔗 [zyyk78/SHTUCodeProxy](https://github.com/zyyk78/SHTUCodeProxy) 和🔗 [saberjack/SHTUCodeProxy](https://github.com/saberjack/SHTUCodeProxy) 

- **cloudflared**：systemd服务，CloudFlare 代理，目前由 zyyk78 控制，cf 提供隧道和代理；DNSHE 提供域名🔗`t80bvm.cc.cd`，其中`elb.t80bvm.cc.cd` 连接到`wechat-bridge`用于维护公众号自动回复 api（公众号由 zyyk78 控制）
  
  这两个账号据使用 zyyk78 的 Github 账号登陆
  
  ⚠️ 该域名为免费，但是需要**每年7月前**续签一次！**一定要有人承接这个功能**
  
  ⚠️ 如果网址发生变动，则需要修改：Cloudflare 后台、公众号后台、文档查询系统中的网址推荐

- **wechat-bridge**：文档查询助手（8089 端口） ，底层接入`shtu-proxy`，上层接入微信公众号，仓库位于📂`git/chatbot.git`
  
  提供文档查询服务、数据刷新（刷新 webview 和本仓库索引）等功能

---

> **特殊事项**：要信仰全能的万机之神，全能的欧姆尼赛亚，机魂不悦则系统可能会出故障 🙏
