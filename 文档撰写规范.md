# 文档撰写规范

本规范基于现有各指南文件归纳而成，用于后续新建和整理文档时统一风格。规则以"保持现有惯例、统一不一致之处"为原则，不另起炉灶。

---

## 1. 语言与语气

- **语言**：简体中文。技术术语、专有名词、命令、代码保留英文原形（如 `Arduino`、`SSH`、`git clone`）。
- **语气**：口语化、亲切，像师兄带新人讲解。可用"说白了""随便选一个""没区别"等表达；允许适度幽默（如"机魂不悦""妈见打进化为妈见夸"），但**不滥用**，确保不影响信息准确传达。
- **人称**：以第二人称"你"为主；涉及实验室整体决策时用"我们"。
- **实用主义**：重操作流程和注意事项，轻理论铺陈。能给出步骤就给步骤，能指链接就指链接，不展开已有教程重复内容。

---

---

## 2. 安全：禁止在文档中放置敏感信息

> ⚠️ **此条为硬约束，违反可能造成实际损失（账号失窃、资金损失、数据泄露），请务必遵守。**

下列信息**严禁**出现在本仓库任何文档（含截图、命令、配置示例、附件链接、表格、注释、commit message）中。

### 必须避免的内容

- **账号凭据**：服务器密码、邮箱密码、Wi-Fi 密码、各类平台登录密码
- **API 凭据**：SSH 私钥、API Token、Access Key / Secret Key、OAuth Client Secret、Webhook URL
- **数据库连接串**：含用户名密码的 `mongodb://`、`postgres://`、`mysql://` 等 URI
- **第三方平台凭据**：PushPlus Token、微信公众号 AppSecret、Cloudflare API Token、GitHub PAT、NAS 后台密码
- **网络基础设施**：校园网账号、网关后台密码、路由器后台密码、域名注册商账号
- **个人信息**：身份证号、手机号、银行卡号、家庭住址、实验室门禁卡号
- **安全敏感信息**：未公开漏洞、内部攻击面、安全机制的绕过方法

### 写作约定

- 命令/配置示例中的密码字段统一用占位符：`<your_password>`、`$PASSWORD`、`*****`、`[REDACTED]` 等，**不要写真密码也不要写示例密码**（即使是"演示"也不行）。
- 截图前**必查**浏览器地址栏、终端 banner、VNC 桌面背景、配置文件内容、IDE 标签页，确认无敏感信息。
- 引用配置文件时只贴必要的非敏感字段；敏感字段用 `# 省略` 替代并注明。
- "测试环境的密码"也视为失守，因为 git 历史无法擦除。

### 凭据已泄露的处置（按优先级）

1. **立即作废**该凭据（改密码 / revoke token / 关闭服务）—— 这一步**优先于**清理历史。
2. 用 `git filter-repo` 或 BFG Repo-Cleaner 清理历史。
3. `git push --force` 覆盖远程历史。
4. 检查仓库的 fork、PR、issues、Actions 日志中是否有缓存。

> **凭据一旦进入 git 历史即视为公开。** 即使后续删除文件，GitHub 上的历史 commit 仍然可以访问，且可能在搜索引擎缓存、第三方备份中存在。

---

## 3. 文档骨架

每个指南文件统一以下结构：

```markdown
# 文档标题

本文中提到的部分路径是基于📁`/mnt/HDD1/archives`的相对路径，如📁`files/example`或📄`models/example.stl`

---

## 一级章节

### 二级章节

（正文）

---
```

规则：

- **首行**为 `#` 一级标题，即文档名。
- **第二段**固定为路径声明（上例那句），告诉读者路径基准。除非该文档完全不涉及路径，否则必填。
- **章节**用 `##`，子章节用 `###`，**最深到 `###`**；如需更深，改用列表或拆分子文档，不要出现 `####`。
- **章节之间**用 `---` 分隔（单独一行，前后各一空行）。
- 段落之间**只留一个空行**，不要连续多个空行。
- 文档结尾的 `---` 之后**不要留大段空行或无内容标题**。

---

## 4. 路径与链接标记

这是本套文档最有特色的约定，**必须严格遵守**。用 emoji 前缀紧贴反引号路径，前缀与反引号之间**无空格**：

| 标记 | 含义 | 示例 |
| --- | --- | --- |
| 📁`路径` | **文件夹/目录**（相对或绝对均可） | 📁`/opt/anaconda3`、📁`files/Miniscope/DAQ` |
| 📄`路径` | **文件**（带扩展名） | 📄`files/cc_settings.json`、📄`models/example.stl` |
| 📂`路径` | **本地绝对路径/大资源目录**（特指非 `archives` 范围内的绝对位置，如权重、家目录） | 📂`/mnt/HDD1/llm/models--Qwen--Qwen3-VL-4B-Instruct` |
| 🔗 | **超链接**（外部 URL、教程、仓库） | 🔗 [Git 教程](https://www.runoob.com/git/git-tutorial.html) |

规则：

- 提到路径时**一律加对应 emoji 前缀**，不要裸用反引号写路径。反引号仅留给命令、代码、变量名。
- 📁 / 📄 / 📂 与反引号之间**无空格**：✅ `📁\`/opt\``，❌ `📁 \`/opt\``。
- 🔗 与链接文字之间**有一个空格**：✅ `🔗 [文字](url)`，❌ `🔗[文字](url)`。
- 同一行内多个路径标记连写时，用中文顿号 `、` 分隔：`📁\`/opt\`、📁\`/mnt/HDD1\`、📁\`/mnt/SSD_tmp\``。
- **路径基准**：默认相对于 `/mnt/HDD1/archives`。若写绝对路径（以 `/` 开头），保持原样即可，读者通过首段声明理解。
- git 仓库路径用 📁（如 📁`git/caiman_scripts.git`），除非是仓库在本地家目录的源码位置才用 📂。

---

## 5. 图片

### 4.1 语法

**统一使用 markdown 图片语法**，不要用 HTML `<img>` 标签：

```markdown
![文件名.png](pictures/对应目录/文件名.png)
```

❌ 禁止：`<img title="" src="..." alt="..." data-align="center">`（历史遗留的 HTML 标签，整理时改写为 markdown 语法）。

### 4.2 路径

- **必须用英文相对路径**：`pictures/英文目录名/文件名.扩展名`。
- ❌ 禁止中文目录、绝对路径、网络路径。
- 图片目录命名与文档对应：`hardware-guide`、`software-guide`、`server-guide`、`projects` 等，**全小写加连字符**。
- 引用前确认文件真实存在。

### 4.3 alt 文本

- **必填**，默认填文件名（含扩展名）：`![vnc.png](pictures/server-guide/vnc.png)`。
- ❌ 不要留空 alt：`![](pictures/.../lcd.png)` → 改为 `![lcd.png](pictures/.../lcd.png)`。
- 若图片有明确语义描述，也可写描述，但保持简短。

### 4.4 位置

图片单独成行，前后各留一空行，与上下文段落分隔。不要把图片塞在句子中间（如 `...完成。![](x.png)` 应拆成两段）。

### 4.5 尺寸与排版（自动应用）

docsify 通过 CSS 自动约束所有图片，**无需在 md 中写尺寸**：

- 图片**不超出正文容器宽度**（`max-width: 100%`），等比缩放
- **居中显示**（`margin: 1.5em auto`），上下留白
- 加 `border-radius: 4px` 圆角与 `box-shadow` 阴影，视觉统一
- 图片所在段落的外边距清零（避免双重空白）

#### 临时"大图"标记

某些场景（PCBA 全景图、流程对比）需要突破宽度限制：在图片语法末尾加 `{.full}`：

```markdown
![pcb-full.png](pictures/hardware-guide/pcb-full.png){.full}
```

- `{.full}` 让 CSS 把图片宽度强制设为 100% 父容器，不再受 80% 之类的限制
- 不到万不得已不要用——会破坏页面视觉一致性
- 一篇文档里出现 3 次以上"大图"基本说明你该把图缩小再上传了

#### 历史 HTML 标签的统一处理

如果老文档里有手写尺寸的 `<img>`：

```html
<img src="..." width="800" data-align="center">
```

统一改写为标准 markdown，让 CSS 接管：

```markdown
![文件名.png](pictures/.../文件名.png)
```

如确需突破尺寸限制，加 `{.full}` 标记。

---

## 6. 强调与代码

| 用途 | 语法 | 示例 |
| --- | --- | --- |
| **加粗** | `**文字**` | **务必阅读**、**不要随便删** |
| *斜体* | `*文字*` | 见下"斜体的两种用途" |
| 行内代码 | `` `代码` `` | `git clone`、`GND`、`192.168.5.5` |
| 代码块 | ` ```bash ` 等带语言标注 | 见下 |

### 加粗

用于**关键警告、易错点、必须执行的动作**。不要泛用，加粗多了等于没加粗。

### 斜体的两种用途（保持一致）

1. **引用本文档内其他章节名**：`见*硬件加工指南*章节`、`（见*UAC 用户访问控制*）`。章节名用斜体，是否带"章节"二字**统一不带**，直接 `*章节名*`。
2. **补充说明/可选项注释**：`*如果没有 Basler 相机则不需要*`、`*其原理是：...*`。

### 代码块

```bash
ssh 账户名@IP  # 例如: ssh zyyk78@192.168.5.5
```

- 代码块**标注语言**（`bash`、`python` 等）。
- 行内命令、路径片段、文件名、配置键名一律用行内反引号，不要裸写。

---

## 7. 列表与缩进

### 无序列表

- 用 `-`（短横线），**不用** `*` 或 `+`。
- 项与项之间：简短并列时**不留空行**；每项为多段长文本时，项间**留一空行**以增强可读性。同一列表内**风格一致**。
- 列表项续行（同一项的后续段落）缩进 **2 个空格**对齐上一项文字。

```markdown
- **第一项**
  
  这是第一项的补充说明，缩进 2 空格。

- **第二项**
```

### 有序列表

- 用于**操作步骤/流程**，必须按顺序执行时才用。
- 数字加点：`1.` `2.`，Markdown 会自动编号，但建议手写连续数字便于阅读源码。
- 步骤间**留空行**，每步若有多段说明，续行缩进 3 空格对齐步骤文字。

### 嵌套

- 嵌套子列表缩进 **2 个空格**，与父项续行对齐。
- 不要嵌套超过 3 层，过深就拆章节。

---

## 8. 警告与提示符号

统一用以下 emoji 前缀，**前缀后空一格**再接文字：

| 符号 | 用途 | 示例 |
| --- | --- | --- |
| ⚠️ | **警告/危险/易错**（烫伤、触电、数据丢失、不可逆操作） | ⚠️ **不要随便删你不知道是什么的东西！** |
| 💡 | **提示/推荐做法** | 💡 推荐使用 Delrin 进行 CNC 切削 |
| 🔗 | 超链接（见第 3 节） | 🔗 [教程](url) |

规则：

- 统一用 **⚠️**（U+26A0 U+FE0F，带 emoji 选择符），**不要**用裸 `⚠`。
- ⚠️ 后**空一格**再接文字：✅ `⚠️ 注意防烫`，❌ `⚠️注意防烫`、`⚠：注意`、`⚠注意`。
- 警告内容如需强调，整句加粗：`⚠️ **禁止空烧！**`
- 不要在同一处堆叠多个警告符号；连续多条警告就用列表，每条一个 ⚠️。
- 💡 用于"锦上添花"的推荐，不是必须；⚠️ 用于"违反会出事"的硬约束。两者不要混用。
- 其他 emoji（🙏、😄 等）仅用于轻松调侃场合，**不在正文信息密集处使用**。

---

## 9. 标点符号

- **正文用中文全角标点**：`，。：；？！""''（）——……`。
- **代码/路径/命令内用英文半角**，不转换。
- **引号统一用中文弯引号** `""`、`''`，不用直引号 `"` `'`，也不用全角直角引号。
- **冒号**：中文语境用全角 `：`；英文/代码语境用半角 `:`。如 `**内网 IP**：\`192.168.5.5\``（标签后全角冒号）。
- **省略号**用 `……`（六个点），不用 `...` 或 `。。。`。
- **破折号**用 `——`（两个全角），不用 `--`。
- 列表项末尾：若是完整句子加 `。`；若是短语则**不加**。同一列表内**风格一致**。
- 避免句末标点缺失（现有文档多处句子没句号就换行，整理时补齐）。

---

## 10. 中英文混排

- **中文与英文/数字之间加一个空格**（盘古之白）：
  - ✅ `Linux 终端的复制粘贴键`、`Arduino 板`、`Ubuntu 24.04`、`1000 rpm`
  - ❌ `Linux终端`、`Arudino开发板`、`1000rpm`
- **例外**：英文与中文标点之间不加空格（如 `LED等设备` 中 `LED` 后接 `等`，中间空格可有可无，保持自然即可）。
- **单位与数字间加空格**：`150 ℃`、`2 h`、`5 mm`、`70 ℃以上`。但温度符号在现有文档中常写作 `70℃`，整理时统一为 `70 ℃`。
- 百分号紧贴数字：`80%`（不加空格）。

---

## 11. 交叉引用

### 11.1 库内文档引用（**斜体 + 链接**）

引用本文档库内的**其他文档**时，**必须同时用斜体 + 链接**——斜体提供视觉提示（与正文区分），链接提供跳转。

#### 基本写法

| 场景 | 正确写法 | 渲染效果 |
| --- | --- | --- |
| 引用单个文档 | `[*硬件加工指南*](硬件加工指南.md)` | *硬件加工指南*（可点击） |
| 引用其他文档的某个章节 | `[*软件使用指南/Miniscope 录制程序*](软件使用指南.md)` | *软件使用指南/Miniscope 录制程序*（链接到文档，章节路径作为可读提示） |
| 一句中两个引用 | `见[*硬件加工指南*](硬件加工指南.md)和[*设备使用指南*](设备使用指南.md)` | 见*硬件加工指南*和*设备使用指南* |

#### 规则

- `[*...*]` 中的 `*` 必须**紧贴** `[` 和内容，不能有空格：✅ `[*硬件加工指南*]`，❌ `[* 硬件加工指南*]`。
- 链接目标**只到文件级**，不要试图跨文件跳到具体章节（docsify 的跨文件锚点不稳定）。如需精确跳转，就在该文档内的章节用 `[*章节名*](#章节名)`，前提是该章节在本文件内。
- ❌ 不要再单独写 `*硬件加工指南*` 这种纯斜体——**没有链接的斜体不能跳转**，读者点不动。
- ❌ 不要用加粗 `**章名**` 或引号 `"章名"` 引用其他文档。

### 11.2 同文档内章节引用

引用**同一个文档内**的章节时，仍然用斜体（不必加链接，因为侧边栏/目录会自动生成跳转）：

- ✅ `见*疑难杂症*章节`、`（见*电路分析*）`
- ❌ 不要给章节加 `[章节](#anchor)` 链接，除非你想覆盖默认锚点行为。

### 11.3 外部资源引用

引用外部资源（GitHub、教程、文档）用 🔗 链接，写明来源标题：

- ✅ `🔗 [Git 教程 | 菜鸟教程](https://www.runoob.com/git/git-tutorial.html)`
- 🔗 与链接文字之间**有一个空格**（详见第 3 节）。
- 标题里建议带上来源站点，便于读者判断可信度。

## 12. 表格

- 用于结构化对照（引脚定义、元件列表、路径说明等）。
- 表头与分隔行必填：`| 表头 | 表头 |` 换行 `| --- | --- |`。
- 对齐：默认左对齐；纯数字/短编号列可居中 `|:---:|`；长文本列左对齐 `|:---|`。
- 表格单元格内**不放多行内容**，复杂说明移到表下正文。
- 表格前后各留一空行。

---

## 13. 待办与未完成

- 用引用块标记未完成内容：`> TODO`，单独一行，置于该章节末尾。
- 正文内若需标注临时不确定值，用 `*TODO*` 斜体内联：`定制一系列*尺寸暂不确定,TODO*的金属环`。
- 不要留空标题（如 `### 自动筛选器使用流程` 下无任何内容），要么写 `> TODO`，要么删除该标题。

---

## 14. 校对清单

整理/提交前过一遍，重点排查以下高频错误：

| 错误 | 正确 |
| --- | --- |
| Arudino | Arduino |
| PraseEvent | ParseEvent |
| logevnet | logevent |
| 飞西安 | 飞线 |
| 丞相模块 | 成像模块 |
| 老铁（指烙铁） | 烙铁 |
| MiniScope（产品名） | Miniscope |
| 重复段落（如 PushPlus 重复两遍） | 删除重复 |

其他常见问题：

- 图片路径是否为英文相对路径、文件是否存在、alt 是否填写。
- 路径是否带 📁/📄/📂 前缀，前缀与反引号间无空格。
- ⚠️ 是否带选择符、后是否空格。
- 中英文间是否加空格。
- 是否有裸 `<img>` 标签需改写为 markdown。
- 是否有连续空行、空标题、未闭合的列表。

---

---

## 15. 自动扫描与检查规范

> 本节给出可在本地或 CI 复用的扫描命令。把这些检查汇总到 `scripts/check_docs.sh` 并接入 `pre-commit` 钩子 / GitHub Actions，可以在合并前自动发现格式违规。

### 15.1 错别字扫描

直接复用第 14 节校对清单：

```bash
grep -nE "Arudino|Arudion|PraseEvent|logevnet|飞西安|丞相模块|老铁|MiniScope" *.md
```

期望输出为空。命中则说明文档里残留错别字。

### 15.2 重复词扫描

捕捉"某些某些""随随""的的"等相邻重复字：

```bash
perl -ne 'while (/(\S{2,4})\1/g) { print "$ARGV:$.: $_\n" }' *.md
```

> ⚠️ 误报较多（"重复""提示""互相"等正常词也会触发），需人工过滤。

### 15.3 ⚠ vs ⚠️ 一致性扫描

裸 `⚠`（无选择符 U+FE0F）应统一为 `⚠️`，且后接一格空格：

```bash
grep -nE '⚠[^️ ]' *.md | grep -v '文档撰写规范.md'
```

期望输出为空（`文档撰写规范.md` 中的命中是规则示例，允许存在）。

### 15.4 外部链接缺 🔗 扫描

跨文档 wiki 链接（`[name](动物实验指南.md)`）不需要 🔗；**外部 URL**（`http(s)://`）则必须带 🔗：

```python
import os, re
files = [f for f in os.listdir('.') if f.endswith('.md')
         and f not in ('_sidebar.md', 'index.md', '文档撰写规范.md')]
for f in sorted(files):
    with open(f, encoding='utf-8', errors='replace') as fh:
        for i, line in enumerate(fh, 1):
            for m in re.finditer(r'\[([^\]]+)\]\((https?://[^\)]+)\)', line):
                if '🔗' not in line[max(0, m.start()-20):m.start()]:
                    print(f"{f}:{i}: {line.strip()[:120]}")
```

期望输出为空。

### 15.5 路径缺 📁/📄/📂 前缀扫描

下列反引号路径如果**整段不是命令**，应在前面加 emoji 前缀：

```python
import os, re
files = [f for f in os.listdir('.') if f.endswith('.md')
         and f not in ('_sidebar.md', 'index.md', '文档撰写规范.md')]
patterns = [
    r'`(/opt/[^`]+)`',   r'`(/mnt/[^`]+)`',   r'`(/etc/[^`]+)`',
    r'`(/var/[^`]+)`',   r'`(/usr/[^`]+)`',   r'`(/home/[^`]+)`',
    r'`(~\/[^`]+)`',
    r'`(files/[a-zA-Z][^`]+)`',  r'`(git/[a-zA-Z][^`]+)`',
    r'`(programs/[a-zA-Z][^`]+)`', r'`(models/[a-zA-Z][^`]+)`',
    r'`(\./[a-zA-Z][^`]+)`',    r'`(\.\./[a-zA-Z][^`]+)`',
]
for f in sorted(files):
    with open(f, encoding='utf-8', errors='replace') as fh:
        for i, line in enumerate(fh, 1):
            for pat in patterns:
                for m in re.finditer(pat, line):
                    if not re.search(r'📁|📄|📂', line[max(0, m.start()-10):m.start()]):
                        print(f"{f}:{i}: path={m.group(1)}")
```

> ⚠️ **必须人工复核**：脚本会把"`/usr/bin/systemctl ...`"这种嵌入命令的路径也报出来。如果整段是 visudo 命令模板、bash 示例等"以命令形式出现"的路径，可豁免。

### 15.6 数字/英文与中文之间空格（盘古之白）

```bash
# 数字直接接中文：2028年 → 2028 年
perl -CSD -ne 'while (/(\d)([\x{4e00}-\x{9fff}])/g) { print "$ARGV:$.: $&\n" }' *.md

# 英文字母直接接中文：Linux终端 → Linux 终端
perl -CSD -ne 'while (/([A-Za-z])([\x{4e00}-\x{9fff}])/g) { print "$ARGV:$.: $&\n" }' *.md
```

> ⚠️ 误报率较高（链接里的 URL、表格分隔符、emoji 等），建议只取样人工改，不要无脑全量替换。

### 15.7 互链完整性检查

互链（`[*...*](file.md)`）最容易出的问题有三种：

1. **目标文件不存在**：删了文件但忘了改引用 → 死链
2. **斜体文档名 ≠ 链接目标**：写 `[*动物实验指南*](硬件加工指南.md)` 这种张冠李戴
3. **斜体内的章节路径在目标文件里找不到**：章节改了名/删了，引用没跟着改

#### 检查脚本

```python
import os, re

KNOWN_DOCS = [
    '动物实验指南', '软件使用指南', '硬件加工指南', '设备使用指南',
    '服务器使用指南', '服务器维护指南', '重要工程项目', '文档撰写规范',
]

def extract_headings(filepath):
    return [(len(m.group(1)), m.group(2).strip())
            for line in open(filepath, encoding='utf-8', errors='replace')
            for m in [re.match(r'^(#+)\s+(.+?)\s*$', line)] if m]

files = [f for f in os.listdir('.') if f.endswith('.md')
         and f not in ('_sidebar.md', 'index.md', '文档撰写规范.md')]
all_headings = {f: extract_headings(f) for f in files}

pattern = re.compile(r'\[\*([^\*\]]+)\*\]\(([^)]+)\)')

errors = []
warnings = []
for f in sorted(files):
    with open(f, encoding='utf-8', errors='replace') as fh:
        for i, line in enumerate(fh, 1):
            for m in pattern.finditer(line):
                content = m.group(1)
                target = m.group(2).split('#')[0]
                if not os.path.exists(target):
                    errors.append(f"{f}:{i} [BROKEN] 目标文件不存在: {target}")
                    continue
                # 文档名一致性
                target_doc = next((d for d in KNOWN_DOCS if target.startswith(d)), None)
                if target_doc and not content.startswith(target_doc):
                    errors.append(f"{f}:{i} [MISMATCH] 斜体是 '*{content}*' 但指向 '{target_doc}'")
                # 章节路径校验（仅当含 '/' 时）
                if '/' in content:
                    section = content.rsplit('/', 1)[-1].strip()
                    target_h = [h for _, h in all_headings.get(target, [])]
                    if not any(section == h or section in h or h in section for h in target_h):
                        warnings.append(f"{f}:{i} [SECTION?] '*{section}*' 在 {target} 中找不到匹配标题")

print(f"错误: {len(errors)}, 警告: {len(warnings)}")
for e in errors: print(e)
for w in warnings: print(w)
```

#### 判定标准

- **错误（必须修）**：BROKEN 目标不存在、MISMATCH 张冠李戴
- **警告（建议修）**：SECTION? 章节名找不到——可能是：
  - 标题改名/删除 → 更新引用
  - 标题里有 emoji 但引用没写 → 加 emoji 或简化引用
  - 大小写/全角半角不一致（如 `MiniScope` vs `Miniscope`）→ 统一

> **例外的"错误"**：`文档撰写规范.md` 里的命中通常是第 11 节的规则示例，不是真实链接。检查时记得排除该文件，或人工区分。

### 15.8 推荐接入方式

#### 本地 pre-commit 钩子

把上面的检查汇总到 `scripts/check_docs.sh`，加执行权限，再软链到 `.git/hooks/pre-commit`：

```bash
chmod +x scripts/check_docs.sh
ln -s ../../scripts/check_docs.sh .git/hooks/pre-commit
```

钩子失败时 commit 会被阻止，强制人工 review。

#### CI 集成

在 `.github/workflows/deploy.yml` 的 `build` 步骤之前加一个 `lint-docs` job，发现违规就 fail 但**不阻塞**部署（用 `continue-on-error: true` 留 warning），让维护者按需修复：

```yaml
lint-docs:
  runs-on: ubuntu-latest
  continue-on-error: true
  steps:
    - uses: actions/checkout@v5
    - name: Run doc checks
      run: bash scripts/check_docs.sh
```

### 15.9 图片引用格式检查

图片是文档里最容易踩坑的元素之一——链接死链、图片混在句末、HTML 标签残留，都会让页面观感崩坏。

#### 检查项

| 项 | 期望 | 命令 |
| --- | --- | --- |
| 死链 | 目标文件真实存在 | 见下方脚本 |
| alt 缺失 | `![]()` 必报错 | 见下方脚本 |
| 绝对/网络/中文路径 | 一律禁止 | 见下方脚本 |
| HTML `<img>` 残留 | 改写为 markdown | `grep -nE '<img\\s' *.md` |
| 图片混在句末 | 拆为独立行 | 见下方脚本 |
| 前后空行缺失 | 前后各一空行 | 见下方脚本 |

#### 一站式检查脚本

```python
import os, re

files = [f for f in os.listdir('.') if f.endswith('.md')
         and f not in ('_sidebar.md', 'index.md', '文档撰写规范.md')]

img_md = re.compile(r'!\[([^\]]*)\]\(([^)]+)\)')
img_html = re.compile(r'<img\\s', re.IGNORECASE)

issues = {'broken': [], 'no_alt': [], 'bad_path': [], 'html': [], 'inline': [], 'no_blank': []}

for f in sorted(files):
    with open(f, encoding='utf-8', errors='replace') as fh:
        lines = fh.readlines()
    for i, line in enumerate(lines):
        if img_html.search(line):
            issues['html'].append(f"{f}:{i}")
        for m in img_md.finditer(line.rstrip()):
            alt, path = m.group(1).strip(), m.group(2).strip()
            if not alt: issues['no_alt'].append(f"{f}:{i}: {path}")
            if path.startswith('/') or path.startswith(('http://','https://')) or re.search(r'[\u4e00-\u9fff]', path):
                issues['bad_path'].append(f"{f}:{i}: {path}")
            target = path.split('#')[0].split('?')[0]
            if not target.startswith(('http://','https://')) and not os.path.exists(target):
                issues['broken'].append(f"{f}:{i}: {target}")
            # 图片位置（独立行 + 前后空行）
            line_before = line[:m.start()].strip()
            line_after = line[m.end():].strip()
            if line_before or line_after:
                issues['inline'].append(f"{f}:{i}")
            if i > 0 and lines[i-1].rstrip() != '':
                issues['no_blank'].append(f"{f}:{i} (前无空行)")
            if i < len(lines)-1 and lines[i+1].rstrip() != '':
                issues['no_blank'].append(f"{f}:{i} (后无空行)")

for k, v in issues.items():
    print(f"{k}: {len(v)}")
    for it in v[:3]: print(f"  {it}")
```

#### macOS 元数据文件的坑

`pictures/` 下会有 `._xxx.png` 这种 macOS 资源叉文件。本仓库 `.gitignore` 已用 `._*` 忽略它们，所以 git 里看不到，但本地文件系统扫描时仍会出现在 `pictures/` 目录里。

检查脚本里记得过滤：

```python
for root, dirs, fs in os.walk('pictures'):
    for f in fs:
        if f.startswith('._'): continue  # ← 跳过
        # ...
```

#### 当前基线

48 个图片引用，2026-09 已全部检查通过：

- 48/48 目标文件存在
- 48/48 alt 填写
- 0 绝对路径、0 网络路径、0 中文路径
- 0 HTML 标签残留
- 48/48 图片独立成行、前后空行

### 15.10 执行原则

- **扫描是辅助，不是替代**。规范文档里明确豁免的（代码块内反引号、整段命令、规则示例）需人工判断。
- **不要无脑接受自动修复**。正则匹配只能发现候选违规，最终决定权在人。
- **改完跑一次完整检查**确认无新增违规，再提交。

## 附：快速自查（提交前 30 秒）

1. 首行 `#` 标题 + 第二段路径声明在不在？
2. 路径都带 📁/📄/📂 前缀且无空格？
3. 图片都是 `![文件名](pictures/英文目录/文件名)` 且文件存在？
4. ⚠️ 都带选择符且后有空格？
5. 中英文之间有空格？
6. 章节间用 `---` 分隔？
7. 没有 `####` 及更深标题？
8. 错别字（Arudino 等）已清？

> 拿不准时，参考 `服务器使用指南.md` 和 `硬件加工指南.md` 的写法——这两个文件是当前风格最完整的范本。
